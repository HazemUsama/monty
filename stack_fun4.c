#include "monty.h"


/**
 * _queue - switch to queue
 * @stack: stack
 * @line_number: line number
 */
void _queue(notUsed stack_t **stack, notUsed unsigned int line_number)
{
	inf.flag = 0;
}

/**
 * _stack - switch to stack
 * @stack: stack
 * @line_number: line number
 */
void _stack(notUsed stack_t **stack, notUsed unsigned int line_number)
{
	inf.flag = 1;
}
